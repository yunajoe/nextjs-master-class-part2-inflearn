# [실습 튜토리얼] API Route와 Server Actions (`'use server'`)

## 💡 실습 목표

1. `'use server'` 지시어를 활용하여 클라이언트에서 직접 실행되지 않는 서버 함수(Server Action)를 작성합니다.
2. 별도의 API 엔드포인트(`route.ts`) 없이 서버 함수에서 데이터 처리 및 저장 로직을 수행합니다.
3. 프론트엔드 `<form>`에 서버 함수를 직접 연결하고, Next.js가 네트워크 통신을 처리하는 RPC 스타일의 동작을 확인합니다.

## 1. 프로젝트 폴더 구조 확인

### Server Actions 아키텍처

기존에는 API Route를 생성하고, 클라이언트에서 `fetch()`로 요청을 보내는 방식이 일반적이었습니다.

Server Actions를 사용하면 별도의 `route.ts` 없이도 서버 함수를 정의하고 폼과 연결할 수 있습니다.

```text
src/
└── app/
    ├── layout.tsx               # 앱 최상위 레이아웃
    ├── actions.ts               # Server Actions (서버 로직)
    └── products/
        └── new/
            └── page.tsx         # 상품 등록 UI
```

- `actions.ts`: 서버에서 실행할 비즈니스 로직
- `page.tsx`: 사용자에게 보여줄 상품 등록 화면
- `layout.tsx`: 앱의 공통 레이아웃

## 2. 백엔드 아키텍처: Server Actions 구축

### Step 1. 서버 비즈니스 로직 작성

**파일: `src/app/actions.ts`**

기존 API Route에서는 `request.json()`으로 요청 데이터를 파싱하고, `Response.json()`으로 응답을 반환했습니다.

Server Actions에서는 함수의 매개변수로 데이터를 전달받고, 반환값으로 결과를 전달할 수 있습니다.

```tsx
// src/app/actions.ts
"use server";

// 1. 데이터 규격 정의
interface NewProductData {
  title: string;
  price: number;
}

/**
 * 서버에서 실행되는 상품 등록 함수
 */
export async function createProductAction(formData: FormData) {
  // 1단계: FormData에서 데이터 추출
  const title = formData.get("title");
  const price = Number(formData.get("price"));

  // 2단계: 서버 측 유효성 검사
  if (
    typeof title !== "string" ||
    !title.trim() ||
    !Number.isFinite(price) ||
    price <= 0
  ) {
    throw new Error("상품명과 올바른 가격을 입력해주세요.");
  }

  // 3단계: 가상의 DB 저장 로직
  console.log(`💾 [DB 저장 완료] 상품명: ${title}, 가격: ${price}원`);

  // 4단계: 클라이언트로 반환할 결과
  return {
    success: true,
    message: "시스템에 상품이 성공적으로 등록되었습니다.",
  };
}
```

### 📝 코드 상세 설명

#### 1. `'use server'` 지시어

- Next.js에 해당 함수를 Server Function으로 처리하도록 지시합니다.
- 이 파일의 Server Actions는 서버에서 실행되며, 클라이언트 번들에 함수 구현 코드가 포함되지 않습니다.
- 클라이언트는 함수의 참조를 통해 서버 함수를 호출할 수 있습니다.
- **중요:** 서버에서 실행된다는 것이 곧 완벽한 보안을 의미하지는 않습니다. Server Actions도 외부 요청으로 호출될 수 있으므로 인증, 권한 검사, 입력값 검증이 필요합니다.

#### 2. `FormData` 매개변수

- `<form>`에서 제출한 데이터를 `FormData` 객체로 전달받습니다.
- `formData.get('title')`처럼 `name` 속성을 기준으로 값을 추출합니다.
- `FormData.get()`의 반환값은 `string`, `File`, `null` 등이 될 수 있으므로 서버에서 타입과 유효성을 검증해야 합니다.

#### 3. HTTP 보일러플레이트 감소

- 개발자가 직접 `Request`, `Response`, `request.json()` 등을 작성하지 않아도 됩니다.
- Next.js가 함수 호출을 위한 네트워크 통신을 처리합니다.
- 단, HTTP 통신 자체가 사라지는 것은 아닙니다. Server Actions는 네트워크를 통해 호출됩니다.

## 3. 프론트엔드 아키텍처: UI와 서버 함수 연결

### Step 2. 상품 등록 폼 작성

**파일: `src/app/products/new/page.tsx`**

서버에서 정의한 함수를 import한 뒤, `<form>`의 `action` 속성에 연결합니다.

```tsx
// src/app/products/new/page.tsx
import { createProductAction } from "../../actions";

export default function NewProductPage() {
  return (
    <div
      style={{
        padding: "40px",
        fontFamily: "sans-serif",
        maxWidth: "400px",
      }}
    >
      <h1 style={{ color: "#0070f3" }}>새로운 상품 등록</h1>

      <p style={{ color: "#666" }}>Server Actions를 활용한 상품 등록 폼</p>

      <hr
        style={{
          borderColor: "#eaeaea",
          margin: "20px 0",
        }}
      />

      {/* Server Action을 form의 action에 연결 */}
      <form
        action={createProductAction}
        style={{
          display: "flex",
          flexDirection: "column",
          gap: "15px",
        }}
      >
        {/* name 값은 서버의 formData.get() 키와 일치해야 함 */}
        <input
          type="text"
          name="title"
          placeholder="상품명을 입력하세요"
          required
          style={{
            padding: "12px",
            border: "1px solid #ccc",
            borderRadius: "6px",
          }}
        />

        <input
          type="number"
          name="price"
          placeholder="가격을 입력하세요"
          required
          style={{
            padding: "12px",
            border: "1px solid #ccc",
            borderRadius: "6px",
          }}
        />

        <button
          type="submit"
          style={{
            padding: "14px",
            backgroundColor: "#0070f3",
            color: "white",
            border: "none",
            borderRadius: "6px",
            fontWeight: "bold",
            cursor: "pointer",
          }}
        >
          상품 등록
        </button>
      </form>
    </div>
  );
}
```

### 📝 코드 상세 설명

#### 1. `action={createProductAction}`

기존 SPA 방식에서는 보통 다음과 같은 작업이 필요했습니다.

- `onSubmit` 이벤트 처리
- `e.preventDefault()` 호출
- 폼 데이터 추출 및 변환
- `fetch()` 요청
- 로딩 및 에러 상태 처리

Server Actions를 사용하면 `<form>`의 `action` 속성에 서버 함수를 연결할 수 있습니다.

Next.js가 폼 제출과 서버 함수 호출에 필요한 통신을 처리합니다.

#### 2. 서버 함수 호출과 Hidden Endpoint

Server Actions는 네트워크 통신 없이 서버 함수를 실행하는 마법이 아닙니다.

Next.js는 Server Action을 호출할 수 있도록 내부적으로 식별자와 통신 메커니즘을 관리합니다. 클라이언트에서 해당 함수를 호출하면 Next.js가 서버로 요청을 전송합니다.

- 클라이언트에서는 일반 함수 호출처럼 사용할 수 있습니다.
- 실제 실행은 서버에서 이루어집니다.
- 개발자가 직접 API URL이나 `fetch()` 요청을 작성하지 않아도 됩니다.
- Server Action은 네트워크 요청으로 호출될 수 있으므로, 별도의 API URL이 코드에 보이지 않는다고 해서 비공개 보안 구역인 것은 아닙니다.

즉, **Server Actions는 서버 함수 호출을 추상화한 RPC 스타일의 기능**입니다.

## 4. 시스템 작동 검증

터미널에서 개발 서버를 실행합니다.

```bash
npm run dev
```

브라우저에서 다음 주소로 접속합니다.

```text
http://localhost:3000/products/new
```

### 🟢 테스트 1. UI 렌더링 및 폼 제출

1. 상품명에 `아키텍트 기계식 키보드`를 입력합니다.
2. 가격에 `250000`을 입력합니다.
3. [상품 등록] 버튼을 클릭합니다.

**예상 결과**

서버 터미널에 다음 로그가 출력됩니다.

```text
💾 [DB 저장 완료] 상품명: 아키텍트 기계식 키보드, 가격: 250000원
```

> 현재 예제는 실제 DB에 저장하지 않고 서버 로그만 출력합니다.

### 🟢 테스트 2. Server Actions의 네트워크 통신 확인

1. 브라우저 개발자 도구(F12)를 엽니다.
2. Network 탭으로 이동합니다.
3. 상품 등록 폼을 제출합니다.
4. 발생한 네트워크 요청의 Method, Payload, Response를 확인합니다.

**예상 결과**

- Server Action 호출을 위한 네트워크 요청이 발생합니다.
- Next.js는 Server Action 호출을 POST 요청으로 처리합니다.
- 요청 세부 정보는 Next.js 버전 및 실행 환경에 따라 달라질 수 있습니다.
- 화면이 전체 새로고침되지 않고 처리될 수 있습니다.

**핵심:** 직접 `fetch()`를 작성하지 않았더라도, 클라이언트와 서버 사이의 통신은 실제로 발생합니다.

### 🔴 테스트 3. 서버 측 유효성 검사 확인

1. 개발자 도구에서 가격 입력란의 `required` 속성을 제거합니다.
2. 가격을 비우거나 잘못된 값으로 제출을 시도합니다.
3. 서버 측 유효성 검사가 실행되는지 확인합니다.

**예상 결과**

서버 함수에서 입력값 검증에 실패하면 에러가 발생합니다.

HTML의 `required`는 브라우저 측 검증일 뿐이므로, 개발자 도구나 직접 만든 요청으로 우회할 수 있습니다.

따라서 **서버 측 유효성 검사는 필수**입니다.

> 이 예제는 검증 실패 시 `throw new Error()`를 사용합니다. 실제 서비스에서는 예외를 적절히 처리하고, 사용자에게 에러 메시지를 표시하는 방식으로 구현하는 것이 좋습니다.

---

## 5. 핵심 개념 정리

| 구분             | 기존 API Route       | Server Actions                   |
| ---------------- | -------------------- | -------------------------------- |
| 서버 로직 위치   | `route.ts`           | `'use server'` 함수              |
| 클라이언트 호출  | `fetch()`            | 함수 참조 호출                   |
| HTTP 요청 구성   | 개발자가 직접 작성   | Next.js가 처리                   |
| 요청 데이터 처리 | `Request` 파싱       | `FormData` 등 매개변수           |
| 응답 처리        | `Response` 반환      | 함수 반환값                      |
| 주요 사용 목적   | 명시적 HTTP API 제공 | 앱 내부 서버 작업 및 데이터 변경 |

### 최종 정리

- Server Actions는 API Route 없이 서버 함수를 호출할 수 있도록 지원합니다.
- 클라이언트에서는 일반 함수를 호출하는 것처럼 사용할 수 있지만, 실제 실행은 서버에서 이루어집니다.
- Next.js가 함수 호출에 필요한 네트워크 통신을 처리합니다.
- 이러한 방식은 RPC 스타일의 서버 함수 호출로 이해할 수 있습니다.
- Server Actions도 외부에서 호출될 수 있으므로 인증, 권한 검사, 입력값 검증이 필요합니다.
