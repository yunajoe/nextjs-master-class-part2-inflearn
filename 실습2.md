# [실습 튜토리얼] 11강. `useFormStatus`와 Pending 상태 UI 제어

## 💡 실습 목표

1. 폼 전송 중 발생하는 지연(Latency) 동안 버튼을 비활성화하여 사용자의 중복 클릭을 방지합니다.
2. 수동 상태 관리 없이 `useFormStatus`를 활용해 폼의 전송 상태를 추적합니다.
3. `useFormStatus`의 핵심 규칙인 **부모 폼 내부의 자식 컴포넌트에서 훅을 호출하는 패턴**을 실습합니다.

## 📚 아키텍처 코어: `useFormStatus`의 본질

### 1. 배경: 과거 SPA의 수동 상태 관리

기존 SPA에서는 폼을 전송할 때 `useState`로 로딩 상태를 직접 관리하는 경우가 많았습니다.

```tsx
const [isLoading, setIsLoading] = useState(false);
```

요청 시작 시 `true`로 변경하고, 요청이 끝나면 `finally`에서 `false`로 되돌려야 했습니다.

이러한 상태 관리 코드를 폼마다 반복해서 작성해야 했습니다.

### 2. `useFormStatus`: 폼 전송 상태 추적

React의 `useFormStatus`는 부모 `<form>`의 제출 상태를 제공하는 훅입니다.

내부적으로 Context를 활용하여 폼의 상태를 하위 컴포넌트에서 읽을 수 있도록 합니다.

이를 통해 버튼 컴포넌트가 부모 폼의 전송 상태를 확인하고, 로딩 UI를 표시할 수 있습니다.

### 3. 기본 문법과 4가지 핵심 프로퍼티

```tsx
import { useFormStatus } from "react-dom";

const { pending, data, method, action } = useFormStatus();
```

| 프로퍼티  | 타입                         | 설명                                               |
| --------- | ---------------------------- | -------------------------------------------------- |
| `pending` | `boolean`                    | 부모 폼의 전송 작업이 진행 중인지 여부             |
| `data`    | `FormData \| null`           | 현재 전송 중인 폼 데이터                           |
| `method`  | `string`                     | 폼의 HTTP 메서드. 일반적으로 `'get'` 또는 `'post'` |
| `action`  | `function \| string \| null` | 부모 폼의 `action` 속성에 지정된 값                |

**핵심: `pending`이 `true`일 때 버튼을 비활성화하거나 로딩 UI를 표시할 수 있습니다.**

### 🚨 핵심 규칙: 부모 폼 내부의 자식 컴포넌트에서 호출

`useFormStatus`는 자신이 속한 컴포넌트의 폼 상태를 추적하는 훅이 아닙니다.

**상태를 추적할 `<form>`의 하위 컴포넌트에서 호출해야 합니다.**

잘못된 구조:

```tsx
function ProductForm() {
  const { pending } = useFormStatus();

  return (
    <form action={createProductAction}>
      <button type="submit">등록</button>
    </form>
  );
}
```

위 코드에서는 `useFormStatus`가 해당 컴포넌트가 반환하는 `<form>`의 상태를 읽지 못합니다.

올바른 구조:

```tsx
function SubmitButton() {
  const { pending } = useFormStatus();

  return (
    <button type="submit" disabled={pending}>
      {pending ? "저장 중..." : "등록"}
    </button>
  );
}

function ProductForm() {
  return (
    <form action={createProductAction}>
      <SubmitButton />
    </form>
  );
}
```

## 1. 프로젝트 폴더 구조 확인

### UI 방어선 아키텍처

`useFormStatus`를 활용하기 위해 제출 버튼을 별도의 자식 컴포넌트로 분리합니다.

```text
src/
└── app/
    ├── actions.ts                   # Server Actions
    ├── components/
    │   └── SubmitButton.tsx         # 폼 상태 추적 버튼
    └── products/
        └── new/
            └── page.tsx             # 상품 등록 폼
```

- `actions.ts`: 서버 액션 함수
- `SubmitButton.tsx`: 부모 폼의 전송 상태를 추적하는 버튼
- `page.tsx`: 상품 등록 UI와 폼을 구성하는 부모 컴포넌트

## 2. 핵심 구현: 상태 추적 전용 컴포넌트

### Step 1. SubmitButton 컴포넌트 작성

**파일: `src/app/components/SubmitButton.tsx`**

```tsx
"use client";

import { useFormStatus } from "react-dom";

export default function SubmitButton() {
  // 부모 form의 전송 상태 추적
  const { pending } = useFormStatus();

  return (
    <button
      type="submit"
      disabled={pending}
      className={`
        p-4 border-none rounded-md font-bold transition-all shadow-sm
        flex justify-center items-center
        ${
          pending
            ? "bg-gray-400 text-white cursor-not-allowed"
            : "bg-[#0070f3] hover:bg-blue-700 text-white cursor-pointer"
        }
      `}
    >
      {pending ? (
        <span className="flex items-center gap-2">
          {/* Tailwind CSS 로딩 스피너 */}
          <svg
            className="animate-spin h-5 w-5 text-white"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
          >
            <circle
              className="opacity-25"
              cx="12"
              cy="12"
              r="10"
              stroke="currentColor"
              strokeWidth="4"
            />

            <path
              className="opacity-75"
              fill="currentColor"
              d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
            />
          </svg>
          안전하게 저장 중...
        </span>
      ) : (
        "상품 등록 시스템 가동"
      )}
    </button>
  );
}
```

### 📝 코드 상세 설명

#### 1. `'use client'`

- `useFormStatus`를 사용하는 컴포넌트는 클라이언트 컴포넌트로 구성합니다.
- 버튼의 상태에 따라 UI를 변경하고 상호작용을 처리합니다.

#### 2. `useFormStatus()`

```tsx
const { pending } = useFormStatus();
```

- 가장 가까운 상위 `<form>`의 제출 상태를 가져옵니다.
- 폼의 Server Action이 실행 중이면 `pending`은 `true`가 됩니다.
- 작업이 완료되면 다시 `false`가 됩니다.

#### 3. `disabled={pending}`

```tsx
<button disabled={pending}>
```

- 전송 중 버튼을 비활성화합니다.
- 버튼의 중복 클릭을 통한 반복 제출을 줄일 수 있습니다.
- `pending` 값에 따라 버튼의 색상, 커서, 텍스트를 변경하여 사용자에게 상태를 알려줍니다.

> 버튼 비활성화는 UI 수준의 중복 제출 방지입니다. 서버 측 중복 요청까지 완전히 차단하는 것은 아니므로, 실제 서비스에서는 필요에 따라 멱등성 처리도 고려해야 합니다.

---

## 3. 부모 화면 조립: 폼 UI 업데이트

### Step 2. 상품 등록 폼 수정

**파일: `src/app/products/new/page.tsx`**

```tsx
import { createProductAction } from "../../actions";
import SubmitButton from "../../components/SubmitButton";

export default function NewProductPage() {
  return (
    <div className="p-10 font-sans max-w-md bg-white rounded-lg shadow-md mx-auto mt-10 border border-gray-100">
      <h1 className="text-[#0070f3] mt-0 mb-2 text-2xl font-bold">
        새로운 상품 통제소
      </h1>

      <p className="text-gray-500 text-sm mb-5">
        Pending UI 방어선이 구축된 폼 시스템
      </p>

      <hr className="border-gray-200 my-5" />

      {/* Server Action이 연결된 부모 폼 */}
      <form action={createProductAction} className="flex flex-col gap-4">
        <input
          type="text"
          name="title"
          placeholder="상품명을 입력하세요"
          required
          className="p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-[#0070f3] transition-all"
        />

        <input
          type="number"
          name="price"
          placeholder="가격을 입력하세요"
          required
          className="p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-[#0070f3] transition-all"
        />

        {/* 부모 폼의 상태를 추적하는 자식 버튼 */}
        <SubmitButton />
      </form>
    </div>
  );
}
```

### 📝 코드 상세 설명

#### 1. `action={createProductAction}`

- 폼 제출 시 실행할 Server Action을 지정합니다.
- Next.js가 폼 제출과 서버 함수 호출에 필요한 통신을 처리합니다.

#### 2. `<SubmitButton />`

- 부모 `<form>` 내부에 배치합니다.
- `useFormStatus`를 통해 부모 폼의 `pending` 상태를 읽습니다.
- 전송 중 버튼을 비활성화하고 로딩 UI를 표시합니다.

> **참고:** `createProductAction`은 `void` 또는 `Promise<void>`를 반환하도록 작성합니다. 이전 강의에서 발생한 반환 타입 오류를 해결했다면 `as any` 우회는 필요하지 않습니다.

---

## 4. 시스템 작동 검증: UX 방어선 테스트

터미널에서 개발 서버를 실행합니다.

```bash
npm run dev
```

브라우저에서 다음 주소로 접속합니다.

```text
http://localhost:3000/products/new
```

### 🟢 테스트: Pending UI 및 중복 클릭 방지

1. 상품명과 가격을 입력합니다.
2. [상품 등록 시스템 가동] 버튼을 클릭합니다.
3. Server Action이 실행되는 동안 버튼 상태를 확인합니다.

**예상 결과**

- `pending`이 `true`로 변경됩니다.
- 버튼이 비활성화됩니다.
- 버튼이 회색으로 변경됩니다.
- 로딩 스피너와 `안전하게 저장 중...` 텍스트가 표시됩니다.
- 요청이 완료되면 버튼이 다시 활성화됩니다.

### 💡 추가 검증: Pending 상태를 명확하게 관찰하기

Server Action이 너무 빨리 완료되면 Pending UI가 짧게 나타나 눈에 띄지 않을 수 있습니다.

실습에서 확인하기 쉽도록 Server Action에 임시 지연을 추가할 수 있습니다.

```tsx
await new Promise((resolve) => setTimeout(resolve, 2000));
```

이 코드를 서버 액션 내부에 임시로 추가하면 약 2초 동안 Pending UI를 확인할 수 있습니다.

## 5. 핵심 개념 정리

| 개념                 | 설명                                                       |
| -------------------- | ---------------------------------------------------------- |
| `useFormStatus`      | 부모 폼의 제출 상태를 제공하는 React 훅                    |
| `pending`            | 폼 전송 진행 여부                                          |
| Context              | 폼의 상태를 하위 컴포넌트에서 읽을 수 있도록 하는 메커니즘 |
| 자식 컴포넌트 분리   | `useFormStatus`가 부모 폼의 상태를 읽도록 하는 구조        |
| `disabled={pending}` | 전송 중 버튼 비활성화                                      |
| Pending UI           | 로딩 스피너, 버튼 텍스트 변경 등 진행 상태를 보여주는 UI   |

### 최종 정리

- `useFormStatus`는 폼의 제출 상태를 추적하는 훅입니다.
- 부모 `<form>` 내부의 자식 컴포넌트에서 호출해야 합니다.
- `pending`을 활용하면 별도의 `useState` 없이 제출 중 UI를 구현할 수 있습니다.
- 버튼 비활성화는 사용자의 중복 클릭을 줄이는 UI 방어선입니다.
- 서버 측 검증과 중복 요청 처리는 별도로 고려해야 합니다.
